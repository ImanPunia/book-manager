import { FileDirectiveDirective } from './file-directive.directive';

describe('FileDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FileDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
